package com.jpro.studentsmeetbackend.testcase;

import static org.junit.Assert.*;

import org.junit.BeforeClass;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

import com.jpro.studentsmeetbackend.dao.ForumMessageDAO;
import com.jpro.studentsmeetbackend.model.ForumMessage;

import junit.framework.Assert;



public class ForumMessageDAOTestCase {
	
	@Autowired
	static ForumMessage forumMsg;
	
	@Autowired
	static ForumMessageDAO forumMsgDAO;
	
	@Autowired
	static AnnotationConfigApplicationContext context;

	@BeforeClass
	public static  void init()
	{
		context = new AnnotationConfigApplicationContext();
		context.scan("com.jpro.studentsmeetbackend");
		context.refresh();
		forumMsg = (ForumMessage) context.getBean("forumMsg");
		forumMsgDAO = (ForumMessageDAO) context.getBean("forumMsgDAO");
	}

	@Test
	public void createForumMsgTestCase(){
		
		forumMsg.setUserId("US001");
		forumMsg.setMsgData("This is a general forum open to all for your opinions and concerns");
		boolean flag=forumMsgDAO.createForumMessage(forumMsg);
		Assert.assertEquals("createForumMsgTestCase",true,flag);
	}

}
